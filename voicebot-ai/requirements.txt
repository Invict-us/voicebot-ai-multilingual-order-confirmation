# ═══════════════════════════════════════════════════
# VoiceBot AI — Python Dependencies
# ═══════════════════════════════════════════════════

# ── Web Framework ──
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# ── Twilio ──
twilio==8.12.0

# ── Environment ──
python-dotenv==1.0.0
pydantic==2.6.0
pydantic-settings==2.1.0

# ── Scheduling ──
apscheduler==3.10.4

# ── AI / NLP (Optional but recommended) ──
openai==1.12.0
elevenlabs==0.2.26
deepgram-sdk==2.12.0

# ── Async HTTP ──
httpx==0.26.0

# ── Production DB (Optional upgrade from SQLite) ──
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
alembic==1.13.1

# ── Caching / Queue (Optional) ──
redis==5.0.1
